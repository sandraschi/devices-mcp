"""Integration tests for devices-mcp.

These tests require real services to be running and may be slow.
They are marked with the 'integration' marker and can be skipped with:
    pytest -m "not integration"
"""
